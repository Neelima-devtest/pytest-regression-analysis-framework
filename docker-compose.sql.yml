version: '3.9'

services:
  mysql:
    image: mysql:8.0
    container_name: pytest-mysql
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: pytest_db
    ports:
      - "3306:3306"
